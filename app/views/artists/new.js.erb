$("#artist-modal").find(".modal-content").html("<%= escape_javascript(render 'form') %>");

$("#artist-modal").modal("show");

$("#artist-submit").on("click", function() {
  Rails.fire($("artist-create-form")[0], "submit");
})
